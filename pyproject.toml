[project]
name = "multi-rag"
version = "0.1.0"
description = "Multi-source RAG system"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
